package withNonStaticMethods;

public interface ClassPropertiesInterface06 {
	
	/**
	 * Incrementa una propiedad de la clase (acumulado) en una cantidad 
	 * expresado en amount
	 * @param amount debe ser mayor que 0 y como mucho de 100∂
	 */
	public void incrementAmount(int amount);
}
